# 任务管理系统

基于 Vue 3、Vite、TypeScript 和 Tailwind CSS 的单用户任务管理应用。当前提交完成项目骨架、响应式列表与看板外壳，以及浅色／深色模式的视觉样式。任务增删改查、持久化和看板拖拽将在后续里程碑完成。

## 运行

```bash
npm install
npm run dev
```

执行 `npm run build` 检查类型并构建生产版本。

## 目录

```text
src/
  App.vue     页面布局及视图切换
  main.ts     应用入口
  style.css   Tailwind 与界面样式
```
